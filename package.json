{
  "name": "cy-automation-workflow",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "import:tools": "node scripts/import-tool-sources.mjs",
    "prebuild": "node scripts/sync-site-covers.mjs",
    "build": "pnpm --dir site build",
    "dev": "pnpm --dir site dev",
    "test": "node --test tests/**/*.test.mjs site/test/**/*.test.mjs"
  }
}
